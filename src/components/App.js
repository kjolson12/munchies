import React from 'react';

import SearchBar from './SearchBar';
import SortBy from './SortBy';

const App = () => {
    return (
        <div>
            <SortBy />
            <SearchBar />
        </div>
    );
};

export default App;